{% extends 'base.html.twig' %}

{% block title %}Accessoires index{% endblock %}

{% block contenu %}
    <h1>Accessoires index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for accessoire in accessoires %}
            <tr>
                <td>{{ accessoire.id }}</td>
                <td>{{ accessoire.nom }}</td>
                <td>
                    <a href="{{ path('accessoires_show', {'id': accessoire.id}) }}">show</a>
                    <a href="{{ path('accessoires_edit', {'id': accessoire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('accessoires_new') }}">Create new</a>
{% endblock %}
