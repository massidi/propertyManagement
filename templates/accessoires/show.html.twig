{% extends 'base.html.twig' %}

{% block title %}Accessoires{% endblock %}

{% block body %}
    <h1>Accessoires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accessoire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ accessoire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('accessoires_index') }}">back to list</a>

    <a href="{{ path('accessoires_edit', {'id': accessoire.id}) }}">edit</a>

    {{ include('accessoires/_delete_form.html.twig') }}
{% endblock %}
