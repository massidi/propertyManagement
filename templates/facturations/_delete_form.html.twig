<div class="text-right">

<form method="post" action="{{ path('facturations_delete', {'id': facturation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facturation.id) }}">
    <button class="btn pull-right">Annuler</button>
</form>

</div>