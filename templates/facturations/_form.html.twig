{{ form_start(form) }}


<div class="row">
    <div class=" col-md-6">
        <div class="form-group">
            {{ form_label(form.paymentAt, 'Date du paiement') }}
            {{ form_widget(form.paymentAt) }}
        </div>
    </div>

</div>

<div class="row">
    <div class=" col-md-6">
        <div class="form-group">
            {{ form_label(form.taxe, 'TVA',{'label_attr': {'class': 'bmd-label-floating'}}) }}
            {{ form_widget(form.taxe) }}
        </div>
    </div>
    <div class=" col-md-6">
        <div class="form-group">
            {{ form_label(form.modeReglement, 'Mode de réglement',{'label_attr': {'class': 'bmd-label-floating'}}) }}
            {{ form_widget(form.modeReglement) }}
        </div>
    </div>
</div>

{{ form_row(form._token) }}

            <button class="btn btn-success  pull-left ">{{ button_label|default('Confirmer Payement') }}</button>

            {{ include('facturations/_delete_form.html.twig') }}


{{ form_end(form) }}