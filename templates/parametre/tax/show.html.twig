{% extends 'base.html.twig' %}

{% block title %}Taxe{% endblock %}

{% block contenu %}
    <h1>Tax</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tax.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tax.nom }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ tax.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tax_index') }}">back to list</a>

    <a href="{{ path('tax_edit', {'id': tax.id}) }}">edit</a>

    {{ include('parametre/tax/_delete_form.html.twig') }}
{% endblock %}
